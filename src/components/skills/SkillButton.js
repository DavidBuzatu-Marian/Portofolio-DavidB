import React from 'react';

const SkillButton = ({ btnText }) => {
  return (
    <button className='button btn btn-round-green mr-32 mt-32'>
      {btnText}
    </button>
  );
};

export default SkillButton;
